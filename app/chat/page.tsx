// TalkToBook — chat route (blueprint Phase 5: app/chat/page.tsx).
// ChatInterface owns the full-screen RTL layout (top bar, messages, input).
import ChatInterface from "@/components/ChatInterface";

export default function ChatPage() {
  return <ChatInterface />;
}
