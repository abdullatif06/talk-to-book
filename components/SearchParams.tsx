// TalkToBook — extracted params display (blueprint Phase 5 & 9).
// Day 1 placeholder; later days render the extracted TravelParams chips.
import type { TravelParams } from "@/types";

export default function SearchParams({ params }: { params: Partial<TravelParams> }) {
  void params;
  return null;
}
